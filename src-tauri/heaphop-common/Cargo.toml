[package]
name = "heaphop-common"
version = "0.1.0"
edition = "2021"

[dependencies]
aya-log-common = "0.1"
aya = { version = "0.12", optional = true }
serde = { version = "1", features = ["derive"], optional = true }

[features]
user = ["aya", "serde"]
